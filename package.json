{
  "name": "weather-app",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "Glassmorphism weather app on Vercel (OpenWeatherMap)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "dev": "vercel dev"
  }
}
