# Ensure text files are diffed as text (emoji-containing markdown was being
# auto-detected as binary by git). No end-of-line forcing to avoid churn.
*.md   text -binary
*.css  text -binary
*.js   text -binary
*.json text -binary
*.html text -binary
*.mjs  text -binary
